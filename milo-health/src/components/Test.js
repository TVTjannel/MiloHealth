import React from 'react'; // get the React object from the react module

class Test extends React.Component {
  render() {
    return <p>Hello, world!</p>;
  }
}

export default Test; // expose the HelloWorld component to other modules
